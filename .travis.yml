language: cpp
compiler:
  - clang
  - gcc
dist: xenial
script: 
  - mkdir build
  - cd build
  - cmake ..
  - make && make test
  
  matrix:
    include:
      - compiler: gcc
        env: CC="g++ -std=c++11"
      - compiler: clang
